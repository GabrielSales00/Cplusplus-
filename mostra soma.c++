#include <iostream>

int main ()
{
    int x, y, sum;
    std::cout << "Os dados, agora: ";
    std::cin >> x;
    std::cout << "outro, agora! ";
    std::cin >> y;
    std::cout << "okay, você passou... \n";
    std::cout << "A soma de " << x << " e " << y << " é " << x + y << "\n";
    return 0;
}